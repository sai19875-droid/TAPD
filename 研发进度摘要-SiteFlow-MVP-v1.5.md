# SiteFlow MVP 研发进度摘要（v1.5）

> 场景：阶段四「研发跟进」周报/进度摘要，面向技术负责人、测试、设计及干系人。
> 周期：W1–W12 路线图收官段（W10–W12 B2C 打磨 + 双后端收敛 + 内测准备）
> 整体状态：**🟢 评审/内测就绪**

## 模块进度

| 模块 | 范围 | 状态 | 关键证据 |
|------|------|------|---------|
| M1 多语言建站生成器 | 模板/翻译降级/SSR | ✅ 完成 | 翻译失败诚实降级；10 语言 + RTL；SSR 直出文案 |
| M2 批发价权限体系 | tier 定价/角色显价 | ✅ 完成 | 未登录看零售价、代理看专属批发价 |
| M3 代理站生成 | 复制商品+批发价/订单回流 UI | ✅ 完成 | storefront checkout → `POST /api/orders` 回流 |
| M4 订单回流 | 写库/佣金预估/汇总/看板 | ✅ 完成 | 11+ 笔订单真实落库，佣金规则触发 |
| M5 轻量拖拽建站 | 区块拖拽/实时预览/前台渲染 | ✅ 完成 | 六类区块按序渲染 + 真实商品 + RTL |
| B2C 购物车+合并结算 | Zustand/持久化/全站抽屉 | ✅ 完成 | 多商品合并下单复用 M3/M4，8/8 回归通过 |
| **B2C 税费/运费** | 服务端权威计费/佣金按总额 | ✅ 完成 | `lib/billing.ts`；7/7 边界值通过 |
| **双后端收敛** | api 补种页面/Order 镜像/只读聚合 | ✅ 完成 | api `tsc` 0；`GET /api/v1/orders` 同源聚合验证 |

## 质量门禁
- **编译**：web `tsc --noEmit` 0 errors；api `tsc --noEmit` 0 errors。
- **回归**：计费边界值 **7/7 PASS**；B2C 合并结算端到端 **8/8 PASS**；种子数据校验 **无缺口**。
- **代码一致性**：web `src` 无遗留 `pageType`(DB)/`.language`(SitePage)/`parentSiteId`(User) 错位引用。

## 阻塞与规避
- **TAPD 连接器未授权** → 改在 GitHub `sai19875-droid/TAPD` 建事项 #1/#2/#3（已打标、关联依赖）。
- **tdrive `file_size` 工具 bug** → 交付物改存 GitHub 仓库（含 README 索引），tdrive 待工具修复后补录。
- 上述两项均**已规避**，不阻塞内测启动。

## 下一步
1. 招募 A/B 角色种子用户，发放 `种子用户内测指南.md`。
2. 按 `内测启动清单.md` 启动 beta，监控订单回流与计费准确性。
3. 内测反馈经 issue #3 评论区回收，阻断性 Bug 建项指派。
4. 内测通过后进入「上线复盘」（阶段六）。

## 遗留待办（需人工/外部）
- [ ] 加 issue 关注人（待技术/测试/设计 GitHub handle）。
- [ ] tdrive 资料库补录（待工具修复或手动上传）。
- [ ] 后续需求候选项见 `后续需求候选项-SiteFlow-MVP.md`（多币种、佣金结算、自由画布等 P1）。
